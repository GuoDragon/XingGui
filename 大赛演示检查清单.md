# 星轨大赛演示检查清单

## 演示前 10 分钟

- 确认 MySQL 已启动，数据库 `xinggui` 可连接。
- 运行 `.\启动后端.bat`，看到 `XingGui backend starting` 和端口、数据库、上传目录信息。
- 浏览器或终端访问 `http://127.0.0.1:8080/health`，确认 `database=UP`、`uploadDirWritable=true`。
- 访问 `http://127.0.0.1:8080/metrics/basic`，确认接口可返回 JSON。
- 确认 Android 模拟器使用 `app/src/main/assets/data/app_config.json` 中的 `http://10.0.2.2:8080`。

## 账号登录检查

- `parent001 / parent001`：可进入家长身份，默认儿童为 `child001`。
- `teacher001 / teacher001`：可进入教师身份，可切换多个儿童。
- `parent002 / parent002`：可进入家长身份，默认儿童为 `child002`。
- `1 / 1`：后端账号可用，但 App 应提示“无移动端入口”。

## 五个主 Tab

- 报告：能看到当前儿童成长概览、雷达图或清晰空状态。
- 档案：能查看打卡项目，完成一次打卡后报告可更新。
- 目标：能查看学期、月度、周目标；IEP 上传入口可打开。
- 资源：能展示推荐、分类、搜索和资源详情。
- 我的：能看到账号资料、儿童资料、隐私与数据说明、退出全部设备。

## IEP 上传检查

- 正常 PDF：上传成功，目标计划刷新，文档状态为 `PASSED_BY_RULES`。
- 空文件或超大文件：提示友好错误，不留下数据库脏记录。
- 扩展名与内容不一致：后端拒绝并记录审计。
- 上传后查看 `/metrics/basic`：`uploadCount` 和 `auditLogCount` 应增加。

## 现场故障快速处理

- 登录提示无法连接：确认 `启动后端.bat` 窗口仍在；模拟器地址应为 `10.0.2.2:8080`。
- Gradle 显示 `83% EXECUTING`：这是后端常驻运行，不是卡死。
- 端口占用：关闭旧的后端窗口，或设置 `XINGGUI_PORT` 后同步修改 `app_config.json`。
- 数据不一致：保留用户新增数据的前提下，重启后端会修复演示账号、角色和儿童关系。
- 依赖下载失败：优先使用赛前已构建好的 Gradle 缓存，避免现场临时联网。
