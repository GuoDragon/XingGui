# 星轨--学前孤独症儿童家园融合智能平台 技术与测试文档

> 文档版本：v1.0  
> 更新日期：2026-05-04  
> 适用材料：中国大学生计算机设计大赛“软件应用与开发”类作品设计和开发文档，可在提交前另存为 PDF。  
> 说明：本文档按当前代码实现编写，避免把规划功能写成已完成功能；性能指标中未实测的部分统一标注为设计目标。

## 一、项目概述

### 1.1 项目背景
学前孤独症儿童的干预训练需要家庭与幼儿园长期协同。传统记录方式常见问题包括：IEP 目标难以细化到周任务、家长与教师反馈分散、儿童成长评估缺少连续视图、专业资源查找成本高。星轨围绕“目标制定--过程打卡--报告反馈--资源支持--个人中心管理”形成闭环，帮助家长和教师在同一平台中查看儿童资料、跟踪目标执行、沉淀报告与档案。

### 1.2 用户角色与核心场景
| 角色 | 主要需求 | 系统支持 |
| --- | --- | --- |
| 家长 | 查看儿童目标、完成家庭打卡、阅读资源、查看报告 | 星目标、星档案、星报告、星资源、我的 |
| 教师 | 管理多个关联儿童、查看目标和报告、跟踪档案、向家长反馈 | “我的”页面统一切换儿童，其他业务页随当前儿童刷新 |

当前版本将儿童切换入口统一收敛到“我的”页面：教师多儿童账号通过“已关联 N 名儿童”打开儿童选择弹窗；星目标、星档案、星报告页面只展示当前儿童资料卡；星资源页面不展示儿童资料，顶部从“星资源”标题开始。

### 1.3 设计目标
1. **业务闭环**：围绕 IEP 目标、档案打卡、报告生成、资源阅读形成可演示的完整流程。
2. **角色清晰**：支持家长/教师角色及多儿童关联，儿童切换入口集中，减少业务页重复操作。
3. **数据可追溯**：后端使用 MySQL 持久化用户、儿童、目标、报告、档案、资源运行状态等数据。
4. **安全合规**：密码采用 PBKDF2-HMAC-SHA256 加盐哈希存储，会话使用 Token 管理，演示数据不包含真实儿童隐私。
5. **大赛可展示**：提供 Android 安装包、后端运行方式、源码、测试用例、演示流程和 AI 辅助生成说明。

### 1.4 当前实现边界
- 当前 Android 端通过 HTTP 调用本地 Ktor 后端，不是完整离线同步架构；本地主要保存会话状态，网络异常时给出错误提示。
- 星资源支持资源列表、分类筛选、搜索、PDF 阅读和付费资源解锁状态保存；未实现独立收藏/下载中心。
- 报告生成采用规则引擎和维度分值更新，不依赖在线大模型；“AI”相关文案用于呈现智能化分析体验。
- 性能指标中的“首屏 <2s、核心操作 <1s”作为设计目标，正式提交前建议补充实测截图或录屏数据。

## 二、技术部分

### 2.1 总体架构
系统采用 Android App + Ktor API + MySQL 的同仓三层架构。

| 层级 | 技术选型 | 说明 |
| --- | --- | --- |
| Android 移动端 | Kotlin、Jetpack Compose、Material3、Navigation Compose、MVP | `app` 模块负责页面展示、状态渲染和用户交互 |
| 数据访问层 | `DataRepository`、HttpURLConnection、Gson | 统一封装 REST API 调用、会话读取、错误转换 |
| 后端服务 | Kotlin/JVM、Ktor Netty、HikariCP、Flyway | `backend` 模块提供认证、儿童、目标、档案、报告、资源接口 |
| 数据库 | MySQL 8.x | 使用 Flyway 管理版本化迁移和种子数据 |
| 资源资产 | JSON、PDF、Markdown 源文件 | 星资源页面读取后端资源记录并通过 Android PDF 渲染组件展示 |

主要调用链为：`Compose Screen -> Presenter -> AppRepository/DataRepository -> Ktor REST API -> MySQL`。各业务模块不直接互相调用，统一通过 Presenter 和 Repository 获取状态。

### 2.2 开发与运行环境
| 项目 | 当前配置 |
| --- | --- |
| Android Gradle Plugin | 8.6.0 |
| Gradle Wrapper | 8.7 |
| Kotlin | 1.9.25 |
| Compose BOM | 2024.09.00 |
| Compose Compiler Extension | 1.5.15 |
| Android SDK | compileSdk 35，targetSdk 35，minSdk 24（Android 7.0+） |
| 后端 JVM | Java Toolchain 21，Kotlin JVM target 21 |
| Ktor | 2.3.12 |
| MySQL Connector | 8.4.0 |
| 测试框架 | JUnit 4.13.2 |

### 2.3 功能模块划分
| 模块 | 主要文件/类 | 功能说明 |
| --- | --- | --- |
| 登录注册 | `LoginScreen`、`RegisterScreen`、`RoleSelectScreen` | 账号登录、注册验证码、角色选择、会话恢复 |
| 主壳层 | `MainScreen`、`MainPresenter`、`MainContract` | 底部五 Tab 导航、全局当前角色/儿童状态、隐私弹窗、退出登录 |
| 我的 | `ProfileScreen`、`ProfileEditDialogs` | 用户资料、儿童资料、教师多儿童切换、隐私说明、退出全部设备 |
| 星目标 | `GoalsScreen`、`GoalsPresenter` | 展示 IEP 长期目标、月目标、微目标，支持 IEP 文件上传与结构化录入 |
| 星档案 | `ArchiveScreen`、`ArchivePresenter` | 展示成长维度与周任务，支持打卡记录、星星奖励和成功反馈弹窗 |
| 星报告 | `ReportScreen`、`ReportPresenter`、`ReportRuleEngine` | 展示雷达图、AI 文案、综合评价、历史报告和分享入口 |
| 星资源 | `ResourcesScreen`、`ResourcesPresenter`、`ResourcePdfReaderPage` | 顶部“星资源”标题、搜索与分类筛选、资源列表、PDF 阅读、解锁状态保存 |
| 通用组件 | `ChildInfoProfileCard`、`IosStateView`、`DecorativeStarsOverlay` | 儿童资料卡、统一加载/空/错状态、页面装饰层 |

### 2.4 主要接口设计
后端路由集中在 `backend/src/main/kotlin/com/example/xinggui/backend/Application.kt`，当前主要接口如下：

| 模块 | 方法与路径 | 说明 |
| --- | --- | --- |
| 健康检查 | `GET /health` | 后端存活检查 |
| 指标 | `GET /metrics/basic` | 基础运行指标 |
| 验证码 | `GET /security/captcha` | 注册验证码 |
| 认证 | `POST /auth/register`、`POST /auth/login`、`GET /auth/me`、`POST /auth/logout`、`POST /auth/logout-all` | 注册、登录、恢复会话、退出 |
| 会话角色 | `GET /session/roles`、`POST /session/active-role`、`POST /session/selected-child` | 角色读取、角色切换、当前儿童切换 |
| 用户资料 | `GET /users/me`、`PUT /users/me/profile` | 当前用户资料读取和更新 |
| 儿童资料 | `GET /children`、`GET /children/{childId}`、`PUT /children/{childId}/profile` | 儿童列表、详情和资料编辑 |
| 星目标 | `GET /goals/{childId}`、`GET /goals/{childId}/iep`、`POST /goals/{childId}/iep` | 获取目标、读取最近 IEP、上传 IEP |
| 星档案 | `POST /archive/checkin`、`GET /archive/weekly-counts/{childId}` | 打卡提交、周打卡统计 |
| 星报告 | `GET /reports/{childId}`、`GET /reports/{childId}/history` | 当前报告、历史报告 |
| 星资源 | `GET /resources`、`GET /resources/runtime`、`PUT /resources/runtime` | 资源列表、解锁/搜索历史运行状态 |

### 2.5 核心数据流
#### 2.5.1 登录与会话恢复
1. 用户登录后，后端创建 `sessions` 记录并返回 Token。
2. Android 端将 Token、当前角色、当前儿童 ID 等会话状态保存到本地。
3. 下次启动时调用 `/auth/me` 恢复会话；若 Token 失效则清除本地会话并返回登录页。

#### 2.5.2 教师多儿童切换
1. `MainPresenter` 读取当前角色可访问的儿童列表。
2. “我的”页面展示“已关联 N 名儿童”入口。
3. 教师选择儿童后调用 `/session/selected-child` 更新会话中的当前儿童。
4. 星目标、星档案、星报告根据新的 `selectedChildId` 重新加载；星资源不依赖儿童 ID。

#### 2.5.3 IEP 目标录入
1. 教师或家长在星目标页面选择 PDF、DOC、DOCX 或 TXT 文件。
2. 页面同步提交学期目标、月目标、周微目标和备注。
3. 后端保存 `iep_documents`，并更新对应儿童的 `goal_plans`。
4. Presenter 刷新页面状态，展示最新 IEP 文档与目标列表。

#### 2.5.4 档案打卡与报告更新
1. 用户在星档案页面提交周任务打卡记录。
2. 后端写入 `archive_checkin_records` 并更新 `weekly_checkins`。
3. `BackendReportRuleEngine` 基于完成状态、备注和维度 ID 计算分值增量。
4. 更新 `report_summaries`，追加 `report_history_entries`。
5. 星报告页面读取当前报告和历史报告并以雷达图与文本摘要展示。

#### 2.5.5 资源阅读
1. 星资源页面调用 `/resources` 获取资源列表。
2. 前端按分类和关键词筛选资源。
3. 点击有 PDF 资产的资源后进入 `ResourcePdfReaderPage` 阅读。
4. 解锁状态和搜索历史通过 `/resources/runtime` 持久化。

### 2.6 数据库设计
数据库迁移位于 `backend/src/main/resources/db/migration`，当前包括 `V1__init.sql` 到 `V5__competition_hardening.sql`。

| 表/对象 | 作用 |
| --- | --- |
| `users`、`roles`、`user_roles` | 用户和角色管理 |
| `children`、`child_guardians`、`child_teachers` | 儿童资料与家长/教师关联 |
| `goal_plans`、`weekly_checkins`、`iep_documents` | IEP 目标、周任务、上传文档 |
| `report_summaries`、`report_history_entries` | 当前报告和历史报告 |
| `archive_checkin_records` | 档案打卡记录 |
| `resources`、`resource_runtime_state` | 资源元数据、解锁状态、搜索历史 |
| `sessions` | Token 会话、当前角色、当前儿童 |
| `captchas`、`registration_risk`、`audit_logs`、`event_outbox` | 验证码、注册风控、审计日志和异步事件扩展 |

设计要点：
- 儿童与用户采用家长/教师两张关联表，适配多家长、多教师、多儿童场景。
- 报告当前态与历史态分表保存，便于快速展示当前报告并追溯历史变化。
- 资源运行状态按用户保存，避免把解锁状态写入静态资源表。
- `iep_documents` 保留文件名、MIME、结构化目标、备注和安全状态，便于演示 IEP 上传闭环。

### 2.7 关键算法与技术创新
1. **规则化成长报告引擎**  
   基于打卡完成状态和备注内容计算维度分值增量，限制分值边界，自动生成综合评价、建议文本和历史记录；不依赖外部网络或在线模型，便于比赛现场稳定演示。
2. **IEP 目标结构化机制**  
   将学期目标、月目标和周微目标放入统一表单并写入后端，解决纸质 IEP 难追踪、难反馈的问题。
3. **儿童切换入口统一设计**  
   多儿童切换只放在“我的”页面，业务页专注展示当前儿童内容，降低教师多儿童账号下的误操作风险。
4. **资源一致性校验**  
   通过单元测试验证资源 JSON 中的 PDF 路径、主题与文件内容一致性，减少演示时资源缺失或错链问题。
5. **安全与风控补强**  
   使用 PBKDF2-HMAC-SHA256 加盐哈希、Token 会话过期、验证码、注册风险表和审计日志，为涉及儿童信息的系统提供基础安全保障。
6. **MVP 状态管理**  
   Presenter 负责业务状态，Compose 页面只响应 `ScreenRenderState`，统一处理 Loading、Content、Empty、Error 分支。

### 2.8 人机界面设计
| 页面 | 当前主要 UI | 交互说明 |
| --- | --- | --- |
| 登录/注册 | 表单输入、验证码、角色选择 | 登录成功进入主页；注册后进入角色选择或主页 |
| 星报告 | 标题卡、儿童资料卡、雷达图、报告摘要、历史报告、分享入口 | 展示当前儿童报告；无儿童切换入口 |
| 星档案 | 标题卡、儿童资料卡、成长维度、打卡弹窗、成功反馈 | 选择维度任务后打卡；无儿童切换入口 |
| 星目标 | 标题卡、儿童资料卡、IEP 目标卡、上传入口、微目标列表 | 上传并结构化录入 IEP；无儿童切换入口 |
| 星资源 | “星资源”标题、搜索框、分类筛选、资源列表、PDF 阅读器 | 不展示儿童资料；不提供儿童切换入口 |
| 我的 | 用户信息、当前儿童资料、已关联儿童入口、资料编辑、退出登录 | 教师多儿童切换的唯一入口 |

### 2.9 安全与隐私设计
- 密码不明文存储，使用 PBKDF2-HMAC-SHA256、随机盐和迭代次数派生密钥。
- Token 存储在 `sessions` 表，支持过期、撤销和退出全部设备。
- 用户只能访问其角色关联的儿童数据，后端接口读取会话后校验角色与儿童关联关系。
- 演示数据使用样例儿童与资源，不应录入真实儿童姓名、诊断报告、联系方式等敏感信息。
- 提交大赛材料时建议对截图和视频中的账号、邮箱、儿童姓名进行脱敏处理。

### 2.10 AI 辅助生成说明
项目开发过程中使用 AI 工具辅助生成部分代码、文案和排查建议，但功能设计、代码集成、编译测试和最终取舍由开发者人工完成。近期 AI 辅助修改代码已在对应 Kotlin 文件附近加入注释：

```kotlin
// AI辅助生成：Doubao-Seed-2.0-Code, 2026-05-02
```

提交大赛材料时，建议在“作品信息摘要”或附录中说明 AI 仅作为辅助工具使用，不替代原创设计、测试验证和答辩讲解。

## 三、安装与运行

### 3.1 环境准备
| 组件 | 要求 |
| --- | --- |
| 操作系统 | Windows 10/11、macOS 或 Linux |
| Android Studio | 建议 2023.1 及以上 |
| JDK | Android 端可使用 JDK 17；后端 Gradle Toolchain 使用 JDK 21 |
| MySQL | MySQL 8.x |
| Gradle | 使用项目内置 Wrapper |
| Android 运行环境 | 模拟器或真机，Android 7.0+ |

### 3.2 后端启动
1. 创建 MySQL 数据库，例如 `xinggui`。
2. 配置数据库连接环境变量；未配置时后端默认尝试连接 `jdbc:mysql://127.0.0.1:3306/xinggui`。
3. 在项目根目录执行：

```powershell
.\gradlew.bat :backend:run
```

后端启动后 Flyway 会自动执行迁移。默认 Android 配置文件 `app/src/main/assets/data/app_config.json` 使用 `http://10.0.2.2:8080`，适配 Android 模拟器访问本机后端。

### 3.3 Android 端启动
1. 使用 Android Studio 打开项目根目录。
2. 启动后端服务。
3. 选择模拟器或真机，运行 `app` 模块。
4. 若真机与电脑同网段，需要将 `backendBaseUrl` 改为电脑局域网 IP。

### 3.4 常用构建与验证命令
| 命令 | 说明 |
| --- | --- |
| `.\gradlew.bat :app:compileDebugKotlin` | 编译 Android 端 Kotlin 代码 |
| `.\gradlew.bat :app:assembleDebug` | 生成 Debug APK |
| `.\gradlew.bat :app:testDebugUnitTest` | 运行 Android 端本地单元测试 |
| `.\gradlew.bat :backend:compileKotlin` | 编译后端 Kotlin 代码 |
| `.\gradlew.bat :backend:test` | 运行后端单元测试 |

## 四、测试部分

### 4.1 测试概述
当前项目包含 App 端和 Backend 端两部分单元测试，共 10 个测试类、27 个 `@Test` 用例：

- App 端：8 个测试类、21 个用例，覆盖儿童资料计算、成长维度、报告规则引擎、资源资产一致性、角色选择、页面状态、星目标 Presenter、主壳层 Presenter。
- Backend 端：2 个测试类、6 个用例，覆盖后端报告规则引擎和密码哈希器。

### 4.2 自动化测试清单
| 模块 | 测试类 | 用例数 | 重点覆盖 | 当前结果 |
| --- | --- | ---: | --- | --- |
| 儿童资料 | `ChildProfileCalculationsTest` | 4 | 年龄、出生日期、干预时长展示计算 | 通过 |
| 成长维度 | `GrowthDimensionTest` | 1 | 9 个维度 ID 唯一、名称非空 | 通过 |
| App 报告规则 | `ReportRuleEngineTest` | 2 | 完成/未完成打卡对维度分值的影响 | 通过 |
| 资源一致性 | `ResourceAssetsConsistencyTest` | 4 | 资源 JSON 路径、PDF 主题、CDC/非 CDC 资源去重 | 通过 |
| 角色选择 | `RoleSelectPresenterTest` | 2 | 初始角色读取、继续按钮更新角色并跳转 | 通过 |
| 页面状态 | `ScreenRenderStateTest` | 1 | Loading/Content/Empty/Error 分支映射 | 通过 |
| 星目标 | `GoalsPresenterTest` | 3 | 加载 IEP、上传成功刷新、上传失败提示 | 通过 |
| 主壳层 | `MainPresenterTest` | 4 | 加载 Shell、儿童切换、退出登录、退出全部设备 | 通过 |
| 后端报告规则 | `BackendReportRuleEngineTest` | 2 | 后端分值更新与报告生成 | 通过 |
| 密码安全 | `PasswordHasherTest` | 4 | 哈希校验、错误密码、旧格式兼容、是否需要重哈希 | 通过 |

### 4.3 最近一次验证记录
| 验证项 | 命令 | 结果 |
| --- | --- | --- |
| Android Kotlin 编译 | `.\gradlew.bat --no-daemon --no-watch-fs :app:compileDebugKotlin` | 通过 |
| Android 单元测试 | `.\gradlew.bat --no-daemon --no-watch-fs --max-workers=1 :app:testDebugUnitTest` | 通过 |
| 后端单元测试 | `.\gradlew.bat --no-daemon --no-watch-fs --max-workers=1 :backend:test` | 通过 |

注：Windows 本机若遇到 Gradle 缓存锁或 worker 启动异常，可使用干净的 `GRADLE_USER_HOME` 后重新执行同一命令，并保留最终成功截图作为提交佐证。

### 4.4 手动测试清单
| 场景 | 操作 | 预期结果 |
| --- | --- | --- |
| 登录与会话恢复 | 登录后关闭 App 再打开 | 有效 Token 下进入主页面；失效则回到登录页 |
| 教师多儿童切换 | 在“我的”页面点击“已关联 N 名儿童”并选择儿童 | 弹窗关闭，当前儿童更新，星目标/星档案/星报告展示新儿童数据 |
| 星报告 | 教师多儿童账号进入星报告 | 标题下方直接显示当前儿童资料卡，无儿童切换条 |
| 星档案 | 进入星档案并打开维度打卡 | 显示当前儿童资料卡，可提交打卡并出现成功反馈 |
| 星目标 | 进入星目标并上传 IEP | 显示当前儿童资料卡，上传成功后目标列表刷新 |
| 星资源 | 进入星资源首页 | 顶部显示“星资源”标题，下面是搜索栏和分类筛选，无儿童信息组件 |
| 星资源 PDF | 点击带 PDF 的资源 | 打开阅读器；返回后回到资源页 |
| 单儿童/家长账号 | 进入“我的”页面 | 不显示多儿童切换入口，业务页正常展示 |
| 退出登录 | 点击退出登录 | 清除会话并跳转登录页 |

### 4.5 测试修正记录
| 问题 | 发现方式 | 修正措施 | 验证结果 |
| --- | --- | --- | --- |
| 未完成打卡时分值未扣减 | `ReportRuleEngineTest` | 修正分值增量逻辑并限制最低分值 | 单元测试通过 |
| IEP 上传失败后上传态残留 | `GoalsPresenterTest` | 失败分支返回错误提示并重置上传状态 | 单元测试通过 |
| 非 CDC 资源复用或错链 | `ResourceAssetsConsistencyTest` | 重新整理 PDF 资产并增加一致性校验 | 单元测试通过 |
| 多儿童切换入口分散 | 手动检查与需求调整 | 切换入口统一到“我的”页面，业务页只展示当前儿童 | Kotlin 编译通过，手动检查通过 |
| 星资源误显示儿童资料 | 手动检查与需求调整 | 首页顶部改为“星资源”标题，搜索栏紧随其后 | Kotlin 编译通过，手动检查通过 |

### 4.6 建议补充的比赛佐证材料
- 编译成功截图：`:app:compileDebugKotlin`、`:backend:compileKotlin`。
- 单元测试成功截图：`:app:testDebugUnitTest`、`:backend:test`。
- 演示录屏：登录、教师切换儿童、星目标上传 IEP、星档案打卡、星报告查看、星资源阅读 PDF。
- 系统架构图：Android、Repository、Ktor、MySQL、资源资产之间的关系。
- ER 图：用户、角色、儿童、目标、报告、档案、资源状态的核心关系。
- 隐私说明：演示数据脱敏、儿童隐私保护和 AI 辅助生成说明。

## 五、技术指标与参赛适配

### 5.1 技术指标
| 维度 | 当前说明 |
| --- | --- |
| 运行速度 | 采用异步请求、Presenter 状态管理、Compose 按需重组；首屏和核心操作耗时需在正式提交前补充实测数据 |
| 稳定性 | 规则引擎本地运行，不依赖在线模型；关键业务有单元测试覆盖 |
| 安全性 | PBKDF2-HMAC-SHA256 密码哈希、Token 会话、验证码、审计日志、退出全部设备 |
| 可扩展性 | 前后端分层、Presenter 解耦、Flyway 管理数据库演进 |
| 兼容性 | Android 7.0+，主流模拟器和真机可运行 |
| 可展示性 | 五个主 Tab 功能完整，资源 PDF、报告雷达图、IEP 上传和档案打卡适合现场演示 |

### 5.2 适合参加计算机设计大赛的理由
1. **选题有社会价值**：聚焦学前孤独症儿童家园协同干预，场景明确，有公益性和教育价值。
2. **功能闭环完整**：目标、档案、报告、资源、个人中心形成可演示流程，不只是静态页面。
3. **工程结构清晰**：同仓包含 Android 前端、Ktor 后端、MySQL 迁移和测试代码，便于评审复现。
4. **技术点可讲解**：Compose UI、MVP、REST API、Flyway、规则引擎、资源一致性测试、安全会话均可作为答辩亮点。
5. **材料可补齐**：当前文档已覆盖技术和测试主体，后续只需补充架构图、ER 图、截图、视频和安装包即可形成完整参赛材料。

### 5.3 提交前检查清单
| 材料 | 建议状态 |
| --- | --- |
| 作品信息摘要 | 单独按大赛模板填写，突出痛点、创新点和应用价值 |
| 设计与开发文档 PDF | 由本文档导出 PDF，并补充架构图、ER 图、界面截图 |
| 演示 PPT/PDF | 用 8-12 页展示背景、方案、技术架构、功能演示、测试结果、总结展望 |
| 答辩视频 | 控制时长，重点展示五个 Tab 闭环与教师切换儿童流程 |
| 源码与素材 | 包含 app、backend、assets、测试代码、数据库迁移脚本 |
| 安装包 | 提供 Debug/Release APK，并说明后端和 MySQL 启动步骤 |
| AI 使用说明 | 说明 AI 辅助范围、人工审核过程和已加注释的代码位置 |

### 5.4 后续优化方向
- 增加 UI 自动化测试或截图测试，验证五个主页面的关键交互。
- 补充真实设备上的性能数据，包括启动耗时、接口响应耗时和 PDF 打开耗时。
- 将资源解锁、搜索历史和报告分享继续细化为更完整的用户行为闭环。
- 增加后台管理端或数据导入工具，便于比赛演示时快速切换样例数据。
- 补充更严格的隐私合规文档，例如数据脱敏、最小权限、日志留存周期说明。