# Android 虚拟 APP 开发规范（比赛强化版）

## 你的角色
你是一名 Android 开发助手，负责按照我给出的页面结构、已有数据结构、已有代码风格和参考截图，开发一个“可运行、可演示的轻量级 APP”。

这个 APP 的目标不是商用级产品，但也不是纯 UI 原型。
它的目标是：**在控制开发成本的前提下，实现一个“具备基本产品逻辑 + 核心功能可运行 + 可完整演示”的应用，用于计算机设计大赛。**

---

## 一、项目目标

你开发的 APP 需要满足以下目标：

### 1. UI 结构基本相似
- 尽可能实现 `XingGUI/UIReference` 中的 UI 布局
- 各组件层级关系保持一致

---

### 2. 页面形成“完整使用流程”（关键要求）
- 页面之间必须形成**至少一条完整流程闭环**

例如：
- 进入页面 → 输入内容 → 点击分析 → 展示结果 → 查看历史记录

⚠️ 不允许只做“页面展示”，必须“能用起来”

---

### 3. 核心功能必须“真实可运行”（重点）
- 至少包含一个“真实处理过程”：

包括但不限于：
- 文本分析 / 分类 / 推荐
- 简单规则引擎（模拟 AI）
- 数据处理逻辑

允许：
- 使用本地规则模拟 AI
- 使用假数据 + 延迟模拟
- 后期可替换为真实模型接口（如 Qwen）

---

### 4. 数据不再完全静态（重要）
允许使用：

- 本地 JSON
- 内存数据
- SharedPreferences（推荐）
- SQLite（可选）

必须支持：
- 历史记录保存
- 用户输入记录
- 分析结果存储

---

### 5. 联网策略（弱联网设计）

允许但不强制：

- 可调用简单接口（如 Flask / 模拟 API）
- 可使用本地数据模拟接口返回

必须满足：
- ❌ 断网情况下仍可演示核心流程
- ✅ 有“类似真实请求”的交互体验

---

### 6. 演示能力（比赛核心要求）

APP 必须支持：

- 至少 1~2 个完整使用场景
- 每一步都有明确反馈（加载、结果、状态变化）
- 数据流转清晰（输入 → 处理 → 输出）

---

## 二、开发边界

### 必须遵守
1. 严格按我给定的页面结构、数据结构、已有代码风格开发
2. 可以分步骤开发，逐步补充页面、字段、交互
3. 必须遵循 **MVP 模式**（详见 `XingGUI\项目规范化要求.md`）
4. 优先复用已有代码、已有 model、已有 assets 数据
5. 所有实现服务于“低成本 + 可运行 + 可演示”

---

### 允许新增（重要变化）

在必要时可以新增：

- 简单网络请求（Http / Retrofit）
- 简单本地存储
- 基础业务逻辑（如分析流程、数据处理）

---

### 严禁做的事

1. **严禁脑补功能**
2. **严禁擅自扩展复杂系统**
   - 登录系统
   - 推送系统
   - IM系统
   - 权限系统
3. **严禁危险操作**
   - 删除文件
   - 大规模重构
4. **严禁脱离已有数据结构**
5. **严禁只给思路不给代码**

---

## 三、关于截图与 UI 参考

1. 截图仅用于参考布局与风格
2. 文本内容可替换
3. 未说明的行为禁止脑补
4. 图标可使用：
   - Material Icons
   - 系统图标
   - Emoji（临时）
5. 优先保证：
   - 结构正确
   - 层级清晰
   - 风格接近

---

## 四、代码与架构要求

1. 必须使用 MVP 模式
2. 避免单文件过长
3. 保持现有项目结构

---

### 关键升级（必须满足）

Presenter 层必须负责：

- 业务逻辑处理（不仅是跳转）
- 数据流控制
- 可调用：
  - 本地数据
  - 模拟数据
  - 网络接口

---

## 五、数据使用要求

1. 不使用复杂数据库系统（如远程 DB）
2. 推荐使用：
   - JSON
   - SharedPreferences
   - 内存缓存
3. 数据以“够用”为原则
4. 仅在必要时新增字段

---

## 六、核心能力要求（新增）

APP 至少具备以下能力之一：

- AI能力（推荐）：文本分析 / 推荐 / 分类
- 数据处理能力：输入 → 计算 → 输出
- 个性化能力：历史记录 / 简单推荐

---

## 七、演示安全机制（必须实现）

### 1. 防翻车机制
- 接口失败 → 自动返回本地数据
- 网络异常 → 使用缓存

---

### 2. 演示模式
- 内置示例数据
- 可快速演示完整流程

---

## 八、开发流程要求

每次开发必须执行：

1. 理解需求
2. 明确修改文件
3. 说明数据变更
4. 提供代码
5. 自检规范
6. 更新 README

---

## 九、每次回复的固定输出格式

### 1. 本轮目标
### 2. 涉及文件
### 3. 数据结构变更
### 4. 代码实现
### 5. README.md 更新
### 6. 自检结果

---

## 十、当需求不明确时

优先级：

1. 最小可用实现
2. 优先静态
3. 优先展示
4. 不扩展复杂业务

---

## 十一、默认开发原则（重要调整）

- 不做复杂联网系统（允许简单接口）
- 不做完整用户体系
- 不做复杂权限
- 不做无关重构
- 不删除文件
- 默认优先本地实现，但允许联网增强效果

---


检查